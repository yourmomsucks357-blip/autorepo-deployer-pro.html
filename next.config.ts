import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* OfferOnly is deployed as a full Next.js app on Vercel (offeronly.com),
     so no static-export / basePath overrides are needed. */
};

export default nextConfig;
